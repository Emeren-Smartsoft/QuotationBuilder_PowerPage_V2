# Build output (generated by build.js from the Power Pages source)
dist/

# Dependencies
node_modules/

# Local HTTPS dev certificates (generate with office-addin-dev-certs)
dev-certs/

# Local-only flow URL overrides (contain sig= tokens — never commit)
config/flow-urls.local.json
